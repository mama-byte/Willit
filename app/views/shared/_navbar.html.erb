<% if user_signed_in? %>
    <div class="footer">
            <%= link_to image_tag("home.png", size: 40), dashboard_path %>
            <h2>Willit</h2>
            <%= link_to image_tag("profile.png", size:40), profile_path %>
    </div>
  <% end %>
