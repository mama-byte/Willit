<% if user_signed_in? %>
    <div class="footer">
            <%= link_to image_tag("home.png", size: 30), dashboard_path %>
            <%= link_to image_tag("profile.png", size:30), profile_path %>
    </div>
  <% end %>
